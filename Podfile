source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!
platform :ios, ‘8.0’
target ‘SwiftPushTransition’ do
    

# 页面下拉dismiss的框架，效果不错
pod 'LazyTransitions'                           # Simple framework to add lazy pop and dismiss like in the Facebook orInstagram apps

end
